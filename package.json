{
  "name": "portiva",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "clean": "rimraf dist src-tauri/target",
    "tauri": "tauri",
    "build:desktop": "tauri build",
    "build:windows": "tauri build --bundles nsis",
    "build:windows:single": "tauri build --no-bundle",
    "build:windows:exe": "tauri build --bundles nsis",
    "build:windows:nsis": "tauri build --bundles nsis",
    "build:windows:msi": "tauri build --bundles msi",
    "build:macos": "tauri build --bundles app,dmg",
    "build:macos:app": "tauri build --bundles app",
    "build:macos:dmg": "tauri build --bundles dmg",
    "build:linux": "tauri build --bundles deb,rpm,appimage",
    "build:linux:deb": "tauri build --bundles deb",
    "build:linux:rpm": "tauri build --bundles rpm",
    "build:linux:appimage": "tauri build --bundles appimage"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-search": "^0.16.0",
    "@xterm/addon-serialize": "^0.14.0",
    "@xterm/xterm": "^6.0.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "rimraf": "^6.1.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  }
}
